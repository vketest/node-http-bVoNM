apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/vketest/node-http-bvonm:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/vketest/node-http-bvonm:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: node-http-bvonm
        chartPath: charts/node-http-bvonm
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/vketest/node-http-bvonm"
          image.tag: "{{.DIGEST_HEX}}"
